[package]
name = "aaron-build"
version = "0.1.0"
edition = "2024"
description = "Build-time FlatBuffers code generator and schema tooling for Aaron"
license = "MIT OR Apache-2.0"

[dependencies]
planus-translation = "1.3.0"
planus-codegen = "1.3.0"

[dev-dependencies]
tempfile = "3"
